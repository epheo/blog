---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: fedora35
spec:
  accessModes:
  - ReadWriteOnce
  volumeMode: Block
  resources:
    requests:
      storage: 100Gi 
  storageClassName: lvms-vg1
